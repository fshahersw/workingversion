# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
/infra/app/artifacts/
/infra/lambda/kb-ingest/artifacts/
/.artifacts/kb-ingest/
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
scripts/pipeline/downloads/
.data/

# Secrets — never commit
.env
.env.*
!.env.example
.tmp-corpus-secret.json

# Eval + run artifacts (regenerable)
/eval-baseline-*.csv
*.output

# Git history archive (kept on disk, not tracked)
*.bundle

# Per-user local Claude Code settings (shared config is settings.json)
.claude/settings.local.json

# Playwright MCP browser artifacts (screenshots / snapshots / console logs)
.playwright-mcp/

# Local calibration / probe logs (regenerable)
*-calibration.log

# Local reference/scratch dump (not part of the app)
/officestuff-main/
