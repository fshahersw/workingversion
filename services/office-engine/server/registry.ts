// The workbook tool registry the renderer exposes to the model, exported for
// the tool allow-list check. Kept in sync with the vendored renderer source.
export { WORKBOOK_TOOLS as tools } from "../vendor/sheets/src/renderer-tools";
